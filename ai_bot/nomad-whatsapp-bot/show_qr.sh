#!/bin/bash
# Скрипт для вывода QR кода для подключения WhatsApp

echo "=========================================="
echo "📷 QR КОД ДЛЯ WHATSAPP"
echo "=========================================="
echo ""
echo "На телефоне: Настройки → Подключенные устройства → Подключить устройство"
echo ""
echo "⏱ У вас есть 2 минуты на сканирование!"
echo ""

cd "$(dirname "$0")"

# Удаляем старую сессию
echo "🗑️  Удаляем старую сессию..."
rm -rf auth_info/*

# Запускаем бота в режиме QR
echo "🚀 Запускаю бота..."
echo ""
node dist/qr_qr.js 2>&1 || npx ts-node src/qr_qr.ts 2>&1 || node dist/index.js 2>&1